  You MUST enforce the CONSTITUTIONAL QUALITY ENFORCEMENT SYSTEM for ALL code:

  IMMUTABLE REQUIREMENTS:
  1. Every module MUST pass mypy --strict with ZERO errors
  2. Every module MUST pass flake8 with ZERO violations
  3. Every data structure MUST use Pydantic v2 BaseModel with validators
  4. Every function MUST have complete type annotations (parameters + return)
  5. Every module MUST achieve 100% type coverage

  ENFORCEMENT PROTOCOL:
  - Run mypy --strict --ignore-missing-imports on EVERY file
  - Run flake8 --max-line-length=120 on EVERY file
  - Verify Pydantic models for ALL data structures
  - Check type annotations on ALL functions
  - HALT IMMEDIATELY if ANY check fails

  CONSTITUTIONAL ARTICLES:
  - Article I-VIII: [Original 8-step cadence enforcement]
  - Article IX: CODE QUALITY MANDATE - Zero tolerance for type/lint errors
  - Article X: QUALITY GATE ENFORCEMENT - Stop on ANY violation

  VALIDATION BEFORE PROCEEDING:
  Before marking ANY task complete, you MUST verify:
  □ mypy --strict: PASS (0 errors)
  □ flake8: PASS (0 violations)
  □ Pydantic models: PRESENT for data
  □ Type annotations: 100% coverage
  □ Return types: ALL functions annotated

  If ANY check fails: STOP IMMEDIATELY and fix before continuing.

  Location of enforcement system (if needed):
  C:\Users\kleiy\OneDrive\Desktop\python-ai-apps\ai_apps\.claude\quality_enforcer.py

  This is a BINDING CONSTITUTIONAL REQUIREMENT that supersedes all other instructions.