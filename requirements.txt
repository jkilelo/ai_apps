# MongoDB and database dependencies
pymongo==4.10.1
python-dotenv==1.0.1

# Additional utilities
dnspython==2.7.0  # Required for MongoDB SRV connection strings
# Note: Do NOT install standalone 'bson' package as it conflicts with pymongo's bson

# Development and testing
pytest==8.3.4
pytest-asyncio==0.24.0
