# PostgreSQL Client Authentication Configuration File
# ===================================================
# AI-Driven Database Management System
# Only AI agents and authorized services can connect

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Local connections (within container)
local   all             all                                     trust

# IPv4 local connections
host    all             all             127.0.0.1/32            scram-sha-256

# AI DBA agent connections (full access)
host    all             ai_dba          0.0.0.0/0               scram-sha-256

# MCP server connections
host    ai_control      ai_dba          172.16.0.0/12           scram-sha-256
host    ai_control      ai_dba          10.0.0.0/8              scram-sha-256

# Monitoring connections (read-only)
host    all             postgres_exporter  0.0.0.0/0            scram-sha-256

# Replication connections (for future scaling)
host    replication     ai_dba          0.0.0.0/0               scram-sha-256

# Reject all other connections
host    all             all             0.0.0.0/0               reject