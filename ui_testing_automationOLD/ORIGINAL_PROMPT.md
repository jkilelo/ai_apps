# ORIGINAL PROMPT (Before Enhancement)

Let's implement PHASE2 based on the comprehensive integeration strategies in C:\Users\kleiy\OneDrive\Desktop\python-ai-apps\ai_apps\ui_testing_automation\UI_TESTING_AUTOMATION_MASTER_PLAN.md. Use C:\Users\kleiy\OneDrive\Desktop\python-ai-apps\ai_apps\ui_testing_automation directory to implement the plan. First, think critically like a senior software engineer then break down every task into the smallest implementable sub-task, then create a MASTER_TRACKER whereby you track, update, and persist the tasks and subtasks. Start building all the base and lowest level code which other higher level codes depends on. Follow this paradigm loop:
0. identify a feature that is required
1. build the feature
2. test the feature with real world example
3. if the test pass, update tracker and move to next feature
4. if test fail, identify root cause and fix, then test again (step 2)

For each task and sub-task, you MUST gurantee through a signed contract and provide verifiable evidence that:
1. The code integrates well with the rest of the repo at C:\Users\kleiy\OneDrive\Desktop\python-ai-apps\ai_apps\ui_testing_automation. THIS IS VERY IMPORTANT
2. The code is NOT a duplicated effort: You should never create a code whose functionality duplicates what already exists in C:\Users\kleiy\OneDrive\Desktop\python-ai-apps\ai_apps\ui_testing_automation or any available in-build python modules or any available public librabries in pypi repo. THIS IS VERY IMPORTANT
3. Each python file is a standalone ready to execute module which has at least one example of how to use the module. This example MUST run whenever the file is run in stand-alone mode, i.e. the example should execute under if __name__ == "__main__": ... THIS IS IMPORTANT
4. You won't move to the next task or subtask if you have not fixed an existing issue
5. You will always use the correct virtual environment
6. The code you write is of the highest quality: should be production ready right away, and follows python best practices, like PEP8, DRY, etc
7. You will always document progress by updating the tracker so that should something happen, we're able to resume from last point of failure